import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'oiler-capacitor-ble-sdk-demo-vue',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
